<%- include("partials/header") -%>
    <h1>Home</h1>
    <p><%= H_content %></p>
    <% posts.forEach(function(post){ %>
        <h1><%= post.title %></h1>
        <p><%= post.content.substring(0,100) + "..." %>
            <a href="/posts/<%= post._id %>">Read more</a>
        </p>
    <% }) %>
    <a class="btn btn-lg float" href="/compose" role="button"><b>+</b></a>
<%- include("partials/footer") -%>